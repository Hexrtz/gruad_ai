# Gruad AI - ระบบวิเคราะห์โรคพืช

แอปพลิเคชัน Flutter สำหรับเกษตรกรไทยในการวิเคราะห์และตรวจจับโรคพืช โดยใช้เทคโนโลยี AI (YOLOv8) เพื่อช่วยในการจัดการและดูแลพืชผล

## 🌾 ฟีเจอร์หลัก

### 1. วิเคราะห์เพลี้ยกระโดดสีน้ำตาล (ถ่ายรูป)
- ถ่ายรูปหลายรูปและเก็บไว้ในแกลเลอรี
- วิเคราะห์แต่ละรูปเพื่อนับจำนวนเพลี้ยอัตโนมัติ
- แสดงผลการตรวจจับด้วย bounding boxes บนภาพ
- คำนวณความหนาแน่นของเพลี้ยต่อหน่วยพื้นที่
- แจ้งเตือนเมื่อพบเพลี้ยจำนวนมาก
- วิเคราะห์หลายรูปพร้อมกันและสรุปผลรวม

### 2. ตรวจจับเพลี้ยแบบ Real-time
- เปิดกล้องและตรวจจับเพลี้ยแบบเรียลไทม์
- แสดง bounding boxes บนภาพกล้องแบบสด
- แสดงสถิติจำนวนเพลี้ยที่ตรวจจับได้
- เริ่ม/หยุดการตรวจจับได้ตามต้องการ

### 3. วิเคราะห์สีใบข้าว
- ถ่ายรูปใบข้าวและวิเคราะห์สีอัตโนมัติ
- ตรวจสอบว่าสีเขียวของใบข้าวเป็นไปตามมาตรฐานหรือไม่
- แสดงค่าเฉลี่ยสี RGB และสีเฉลี่ยของใบข้าว
- แสดงผลการวิเคราะห์แบบละเอียด

## 🎨 ฟีเจอร์ UI/UX

- **Splash Screen** ที่สวยงามพร้อม animation
- **Modern UI Design** ด้วย gradient colors และ shadows
- **Responsive Layout** ที่ใช้งานง่ายบนทุกขนาดหน้าจอ
- **Loading States** ที่แสดงสถานะการทำงานอย่างชัดเจน
- **Interactive Cards** พร้อม animations
- **Beautiful Dialogs** สำหรับแสดงผลการวิเคราะห์

## 📋 ข้อกำหนดระบบ

- **Flutter SDK**: ^3.10.4
- **iOS**: 13.0 หรือสูงกว่า
- **Android**: API 21 หรือสูงกว่า
- **Camera Permission**: จำเป็นสำหรับการใช้งาน

## 🚀 การติดตั้ง

### 1. Clone repository
```bash
git clone <repository-url>
cd gruad_ai_new
```

### 2. ติดตั้ง dependencies
```bash
flutter pub get
```

### 3. สำหรับ iOS
```bash
cd ios
pod install
cd ..
```

### 4. รันแอป
```bash
flutter run
```

## 📱 วิธีใช้งาน

### วิเคราะห์เพลี้ย (ถ่ายรูป)
1. เปิดแอปและเลือก "วิเคราะห์เพลี้ย (ถ่ายรูป)"
2. รอให้กล้องเปิดใช้งาน
3. กดปุ่ม "ถ่ายรูป" เพื่อถ่ายรูปเพลี้ย (สามารถถ่ายได้หลายรูป)
4. ระบบจะวิเคราะห์แต่ละรูปอัตโนมัติ
5. กดปุ่ม "วิเคราะห์ทั้งหมด" เพื่อสรุปผลรวม
6. กดที่รูปในแกลเลอรีเพื่อดูผลการตรวจจับพร้อม bounding boxes

### ตรวจจับเพลี้ยแบบ Real-time
1. เปิดแอปและเลือก "ตรวจจับเพลี้ยแบบ Real-time"
2. รอให้กล้องและโมเดลพร้อมใช้งาน
3. กดปุ่ม "เริ่มตรวจจับ"
4. ระบบจะแสดง bounding boxes บนภาพกล้องแบบสด
5. ดูสถิติจำนวนเพลี้ยที่ตรวจจับได้ด้านบน
6. กดปุ่ม "หยุดตรวจจับ" เมื่อต้องการหยุด

### วิเคราะห์สีใบข้าว
1. เปิดแอปและเลือก "วิเคราะห์สีใบข้าว"
2. รอให้กล้องเปิดใช้งาน
3. กดปุ่ม "ถ่ายรูปและวิเคราะห์"
4. ระบบจะวิเคราะห์สีใบข้าวอัตโนมัติ
5. ดูผลการวิเคราะห์ว่าสีเขียวเป็นไปตามมาตรฐานหรือไม่

## 🏗️ โครงสร้างโปรเจกต์

```
lib/
├── main.dart                          # Entry point
├── models/
│   └── pest_result.dart              # Data models
├── screens/
│   ├── splash_screen.dart            # Splash screen
│   ├── home_screen.dart              # หน้าหลัก
│   ├── pest_camera_screen.dart       # หน้าวิเคราะห์เพลี้ย (ถ่ายรูป)
│   ├── realtime_pest_detection_screen.dart  # หน้าตรวจจับ Real-time
│   └── leaf_analysis_screen.dart      # หน้าวิเคราะห์ใบข้าว
├── services/
│   ├── pest_analysis_service.dart     # Service สำหรับวิเคราะห์เพลี้ย
│   └── leaf_analysis_service.dart     # Service สำหรับวิเคราะห์ใบข้าว
└── widgets/
    └── image_with_boxes.dart         # Widget สำหรับแสดงภาพพร้อม bounding boxes
```

## 📦 Dependencies หลัก

- **camera**: ^0.10.5 - สำหรับเข้าถึงกล้อง
- **permission_handler**: ^11.3.0 - สำหรับจัดการ permissions
- **image**: ^4.1.3 - สำหรับประมวลผลภาพ
- **tflite_flutter**: ^0.12.1 - สำหรับรัน TensorFlow Lite models

## 🤖 AI Model

แอปใช้ **YOLOv8n** (YOLOv8 Nano) ที่ fine-tuned สำหรับตรวจจับ:
- Brown Planthopper (เพลี้ยกระโดดสีน้ำตาล)
- Green Leafhopper (เพลี้ยจักจั่นสีเขียว)
- Leaf Folder (หนอนม้วนใบ)
- Rice Bug (มวนเขียวข้าว)
- Stem Borer (หนอนกอ)
- Whorl Maggot (แมลงวันหนอน)

โมเดลไฟล์: `assets/yolov8n.tflite`
Class labels: `assets/labels.txt`

## ⚙️ การตั้งค่า

### iOS
ไฟล์ `ios/Runner/Info.plist` ต้องมี:
```xml
<key>NSCameraUsageDescription</key>
<string>This app needs camera access to take photos for pest and leaf analysis.</string>
```

### Android
ไฟล์ `android/app/src/main/AndroidManifest.xml` ต้องมี:
```xml
<uses-permission android:name="android.permission.CAMERA" />
```

## 🔧 การปรับแต่ง

### ปรับ Confidence Threshold
แก้ไขใน `lib/services/pest_analysis_service.dart`:
```dart
static const double confidenceThreshold = 0.2;
static const double classConfidenceThreshold = 0.15;
static const double objectnessThreshold = 0.15;
static const double nmsThreshold = 0.45;
```

### ปรับสีมาตรฐานใบข้าว
แก้ไขใน `lib/services/leaf_analysis_service.dart`:
```dart
static const double standardGreenMin = 100.0;
static const double standardGreenMax = 200.0;
```

## 📝 License

โปรเจกต์นี้เป็นส่วนหนึ่งของงานพัฒนาเพื่อเกษตรกรไทย

## 👨‍🌾 ผู้พัฒนา

พัฒนาเพื่อเกษตรกรไทย

## 🙏 ขอบคุณ

- YOLOv8 model จาก Ultralytics
- Flutter community
- TensorFlow Lite team

---

**หมายเหตุ**: โมเดล AI ควรได้รับการ fine-tune เพิ่มเติมด้วยข้อมูลจากพื้นที่จริงเพื่อความแม่นยำที่สูงขึ้น
**มีรูปภาพตัวอย่างเป็น file .png**
